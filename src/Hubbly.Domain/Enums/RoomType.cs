namespace Hubbly.Domain.Enums;

public enum RoomType
{
    System = 1,      // Системные комнаты (Новички, Новички 2...)
    Public = 2,      // Публичные комнаты (будут позже)
    Private = 3      // Приватные комнаты (будут позже)
}